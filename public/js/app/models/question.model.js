askmePro.models.QuestionModel = Backbone.Model.extend({
    idAttribute: '_id',
    initialize: function () {
    },
    defaults: {
        
    },
    validate: function (attrs) {
        
    }
});